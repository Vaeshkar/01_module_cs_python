# 1. Left-Left (LL)
def balance(self, node):
    if balance < -1 and                     # ______________
    ((inserted_value is not None and        
      inserted_value < node.left.value) or  
     (deleted_value is not None and         
      self.get_balance(node) < -1 and       
      self.get_balance(node.left) <= 0)     
    ):

    pass